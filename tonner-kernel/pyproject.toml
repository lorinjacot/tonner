[project]
name = "tonner_kernel"
dynamic = [
    "version",
    "description",
    "readme",
    "urls",
    "authors",
    "license",
    "keywords",
]
dependencies = [
    "tonner"
]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
